# MailIQ v5 — Current Screenshot Placeholder

**Status:** CURRENT RUNTIME SCREENSHOT SET INCOMPLETE.

Existing historical architecture imagery is retained elsewhere and must not be relabelled as current runtime proof.

## Current screenshot set required

When v5/current-candidate workflows are configured and rerun, add screenshots for:

- current system/workflow overview;
- onboarding router + one plan path;
- representative tier processor;
- Gmail or Outlook receiver execution;
- provider renewal/token-refresh execution;
- subscription/billing/usage state path;
- notification/delivery path;
- health/admin/DLQ path;
- database/account state before/after a representative run;
- one controlled failure and recovery execution.

Each screenshot should identify the workflow name, date, environment and whether it is `CURRENT RUNTIME`, `TEST EVIDENCE` or `ARCHITECTURE`.

Do not populate this folder with old-version screenshots solely to make it look complete.